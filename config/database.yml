default: &default
  adapter: mysql2spatial
  encoding: utf8
  username: root
  password:
  host: 127.0.0.1
  port: 3306

development:
  <<: *default
  database: mapkeep_dev

test:
  <<: *default
  database: mapkeep_test

